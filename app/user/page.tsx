import Header from '@/components/Header';
import UserProfile from '@/components/backend/user/userProfile';

export default function User() {
  return (
    <main>
      <Header/>
      <UserProfile/>
    </main>
  );
}
